[project]
name = "agent-harness-preference-extractor"
version = "0.1.0"
description = "Portable skill and local extractor for deriving user preferences from supported coding-agent harnesses"
requires-python = ">=3.9"

[tool.unittest]
start-dir = "tests"
pattern = "test_*.py"
